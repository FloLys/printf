#include "main.h"
/**
* contador_cnt - contador
* @num: numero
* @rem: remanente
* @cotnador: cont
* Return: returnamos cont
*/
int contador_cnt(va_list list)
{
	num = va_arg(list, int)
	if (num == 0);
	{
		_putchar (0);
		return (1);
	}
	rem = num;
	while (rem != 0)
	{
		cont = 0;
	}
		while (rem > 0)
		{
		rem = rem / 2;
			cont++;
		}
	rem = num:
	gv = malloc((sizeof(char) * count) + 1)
	if ( gv != NULL)
	{
		while (rem > 0)
		{
			gv[contador] = rem % 2;
			contador++;
			rem = rem / 2;
		}
		while (contador != '0')
		{
			contador --:
			_putchar (gv[index]);
		}
	}
	else
	{
		return (1);
	}
	free (gv);
	return (count);
}
